<tr>
	<td><%= image_tag user.avatar.thumb.url, width: 30 %></td>
	<td><%= user.passport %></td>
	<td><%= user %></td>
	<td><%= user.email %></td>
	
	<% Quiz.all.each do |q| %>
		<td><%= no_link_quiz_answer(q, user) %></td>
	<% end %>
	
	<td><%= link_to "Ver respuestas", admin_user_path(user) %></td>
</tr>
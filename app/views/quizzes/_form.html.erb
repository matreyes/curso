<%= form_for [@quiz, @answer], remote: true do |f| %>
  <%= f.text_area :answer, class: 'span9' %><br><br>
	<%= f.submit "Enviar respuesta", :disable_with => 'Enviando...', class: 'btn success span9', style:'margin-left:5px' %>
<% end %>
<div class="topbar">
	<div class="fill">
		<div class="container">
			<%= link_to "Pasa", levels_path, class: 'brand' %>
			<ul class="nav">
				<% @levels.each do |level| %>
					<% if level == @level %>
					<li class="active"><%= link_to level.name, level %></li>
					<% else %>
					<li><%= link_to level.name, level %></li>
					<% end %>
				<% end %>
			</ul>
			<% if user_signed_in? %>
				<div class="pull-right logged">
					<%= current_user.email %> <%= link_to "Salir", destroy_user_session_path, :method => :delete %>
				</div>
			<% else %>
				<%= form_for "user", :url => user_session_path, :html => { class: "pull-right" } do |f| %>
				  <%= f.email_field :email, class: "input-small", placeholder: "rut" %>
				  <%= f.password_field :password, class: "input-small", placeholder: "contraseña" %>
					<%= button_tag "Ingresar", class: "btn" %>
				<% end %>
			<% end %>
			
		</div>
	</div>
</div>